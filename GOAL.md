# Adia — Build Goals

- [x] Xcode project scaffold: Swift Package + targets (AdiApp, AdiCore, AdiTests), Package.swift, folder structure
- [x] Notch UI: NSPanel subclass positioned in notch area, collapsed state (minimal dot) + expanded state (task info + actions)
- [x] Session model: SessionState enum, task description, success criteria, start time, whitelist
- [x] Session creation view: SwiftUI form for task + criteria input, Go button
- [x] Screen capture pipeline: ScreenCaptureKit SCStream wrapper, emits CGImage every 1-2s
- [x] Claude API client: async/await wrapper for vision + chat completions, reads ANTHROPIC_API_KEY from ProcessInfo (claude-haiku-4-5 fast / claude-sonnet-4-6 strong)
- [x] On-task detection: send screen CGImage to claude-haiku-4-5, classify on-task/off-task/ambiguous with task context
- [x] Callout system: notch expansion triggered on off-task detection, friend-like direct message overlay
- [x] Blocking engine: read/write /etc/hosts to block domains, restore on session end, requires elevated perms
- [x] Blocked page: local NWListener HTTP server serves blocked page on port 80/8080
- [x] Reasoning conversation: SwiftUI chat UI for user to argue for site access; AI grants or denies with context memory
- [x] Task verification: send screen CGImage to claude-sonnet-4-6 with success criteria, returns verified/not-verified + explanation
- [x] Early exit conversation: AI engages user in conversation before confirming exit; user can always override
- [x] Session persistence: encode/decode SessionState to UserDefaults or local JSON file
- [x] Network loss resilience: NWPathMonitor + circuit breaker, offline UI indicators, graceful degradation when API calls fail
- [x] Whitelisted domains visibility: show AI-granted site access in active/paused session notch UI
- [x] Daily focus goal: configurable daily target with progress bar in idle notch + collapsed pill label
- [x] Structured logging: replace all print() calls with AppLogger (info/warning/error) for queryable, structured log output
- [x] Focus insights: analyze session history to surface patterns — avg duration, completion rate, avg focus score, best hour/day, trend detection
- [x] Session reliability tracking: persist pause count, paused duration, and stream failure count in SessionRecord + Session for post-session analytics
- [x] Safety hardening: eliminate fragile force-unwrap in FocusInsights, add structured logging to all persistence and AI parsing fallback paths
- [x] NotchView decomposition: split 1621-line NotchView.swift into focused sub-view files under Views/Notch/
- [x] SettingsView decomposition: split 1795-line SettingsView.swift into 6 focused sub-view files under Views/Settings/
- [x] SessionState decomposition: extract 700+ lines of blocklist data from SessionState.swift into DefaultBlocklists.swift
- [x] CalloutManager decomposition: extract 400+ lines of callout message pools from CalloutManager.swift into CalloutMessages.swift
- [x] AgentAIClient decomposition: extract 210+ lines of response parsers and types from AgentAIClient.swift into AgentAIResponseParser.swift
- [x] SessionManager decomposition: extract activation pipeline, verification, timer, and HapticPlayer from SessionManager.swift (571 → 289 lines)
- [x] Keychain error logging: add structured logging for all SecItemAdd/SecItemDelete failures in LicenseManager and SettingsStore
- [x] ExpandedNotchView decomposition: extract active/paused/verification body views from ExpandedNotchView.swift into ExpandedNotchBodies.swift
- [x] HistoryTab decomposition: extract weekly section, insights section, search bar, and toolbar from HistoryTab.swift into HistoryInsightsSection.swift
- [x] Webhook subscription cancellation bug: setStatusBySub looks up by stripe_sub instead of license key so cancellations actually land
- [x] Seat visibility in app: LicenseManager.fetchSeats() + deactivateMachine(), AccountSettingsTab "Activated Machines" section with per-seat remove button and current-machine badge
- [x] Additional SE Asian blocklist domains: grab.com (food/ride delivery), shopback.com (cashback rabbit hole), carousell.com (secondhand marketplace)
- [x] LicenseManager network tests: MockURLProtocol + urlSession injection; tests for fetchSeats (success/no-op/server-error) and deactivateMachine (success/not-licensed/server-error); daraz.pk + 11street.my blocklist additions
- [x] Admin reactivate endpoint: POST /api/admin/reactivate sets canceled/expired/past_due license back to active; ReactivatePanel in admin UI; 11 tests
- [x] Admin stats endpoint: GET /api/admin/stats returns aggregate counts by plan/status + new-in-7d/30d + activated machines; StatsPanel in admin UI; 8 tests; fix findLicensesByEmail ordering ASC→DESC bug + tighten order test
- [x] Admin search-licenses endpoint + enhanced LookupPanel: GET /api/admin/search-licenses full-text search across key/email/note; lookup route now returns {license, recentAudit}; LookupPanel shows structured card + 5 most-recent audit entries; resend_payment_failed now writes audit log; 18 tests
- [x] machineCount in search results + click-to-lookup: searchLicenses LEFT JOIN on activations; machineCount column in search table; clicking a key auto-fills and triggers LookupPanel with scroll; 3 tests (machineCount=0, machineCount=N, no bleed across rows)
- [x] Admin rate-limit: adminGuard() helper combines 20 req/60s per-IP rate-limit + ADMIN_TOKEN auth; wired into all 26 admin routes; 14 tests
- [x] Template "Edit & Launch" context menu: right-click pinned template in idle notch opens session creation form pre-filled with template task + preferredDuration; left-click direct-launch preserved
- [x] SessionManagerTests @MainActor refactor: add @MainActor to struct, eliminate await MainActor.run{} boilerplate across all 50+ test call sites, make injectSession() synchronous
- [x] Admin bulk-issue endpoint: POST /api/admin/bulk-issue generates 1–50 license keys at once; shared email/plan/note/expiresAt override; BulkIssuePanel with copy-all button; 26 tests
- [x] Admin bulk-transfer endpoint: POST /api/admin/bulk-transfer moves up to 100 keys to a new email; changed + skipped (not_found/already_set); BulkTransferPanel; 20 tests
- [x] Admin panel URL param pre-fill: ?token= pre-fills token input on load; ?section= pre-fills section filter + stays in sync as filter changes (bookmarkable filtered views); token hint tip added to card
- [x] Audit-log pagination + action filter: GET /api/admin/audit-log gains ?offset= + ?action= params; countAuditLog DB function (SQLite + Postgres); response includes total/hasMore/offset/limit; AuditPanel load-more button + action filter input; 13 tests (627 → 636)
- [x] Audit-log since filter + debounce: ?since=YYYY-MM-DD on GET /api/admin/audit-log (SQLite + Postgres); AuditPanel date input + 300ms debounce on key/action/since inputs; CSV export forwards since; 6 tests (636 → 642)
- [x] Expiring-soon endpoint: GET /api/admin/expiring-soon?days=N returns active licenses expiring within N days (max 365), ordered soonest-first; ?plan= filter; ?format=csv export; listExpiringLicenses in db.ts + db-pg.ts + store.ts; ExpiringSoonPanel in admin UI with per-row urgency coloring; 26 tests (642 → 668)
- [x] Admin bulk-revoke tests: 20 tests covering auth (401 no-token, 401 wrong-token, 200 ?token=), validation (missing/empty/>100 keys), core behavior (single/multiple revoke, key normalization, previousStatus preservation, past_due/expired→canceled), skip behavior (not_found, already_revoked, mixed), and audit log (one entry per revoked key, correct detail, no entry for skipped); 673 → 693 tests
- [x] Admin bulk-set-status tests: 25 tests covering auth (401 no-token, 401 wrong-token, 200 ?token=), validation (missing/empty/>100 keys, missing/invalid status), core behavior (all four valid statuses, key normalization, previousStatus, all status transitions), skip behavior (not_found, already_set, mixed, no mutation), and audit log (set_status entry per changed key, detail shape, no entry for not_found/already_set); 712 → 737 tests
- [x] Export endpoint rate limiting + tests: 10 req/60 s per-IP limit (separate bucket from adminGuard) on audit-log-export and export-licenses; 23 tests for export-licenses (auth/validation/empty/data/rate-limit), +2 rate-limit tests for audit-log-export, +1 ?token= auth test for admin-notify; 737 → 763 tests
- [x] Suggested template context menu: right-click on suggested template in idle notch shows "Launch" (direct start) and "Edit & Launch…" (pre-filled form); left-click pre-fill behavior preserved; launchSuggested() mirrors launchTemplate() error handling without recordUse
- [x] Suggested template dismissal + peek tooltip: hover over any suggestion shows its success criteria via .help(); right-click context menu adds "Dismiss" (destructive) to remove that suggestion without hiding the section; "dismiss all" header button dismisses visible suggestions at once; dismissals persist in SettingsStore.dismissedSuggestionTasks (UserDefaults); "Reset dismissed suggestions" button appears in Settings → Templates when any are dismissed
- [x] showSuggestedTemplates toggle reset: re-enabling the suggestions section clears dismissed suggestions so all items appear fresh; didSet calls resetDismissedSuggestions() when value transitions to true; 6 new tests in SettingsStoreTests
- [x] Unpin from notch: right-click context menu on any pinned template in idle notch now shows "Unpin" (destructive, pin.slash icon) below a Divider; removes from local state immediately for responsive UI; persists delete to SessionTemplateStore async; updates idleTemplateCount so collapsed pill state refreshes
- [x] Streak repeat-broken copy: SettingsStore.streakBreakCounts persists per-milestone break counts to UserDefaults; incrementStreakBreak(days:) returns new count; SessionNotifier.streakRepeatBrokenBody(days:breakCount:) shifts tone from surprise to pattern-aware action on second+ break; sendStreakBroken uses repeat copy when breakCount≥2; 12 new tests (6 NotifierTests + 6 SettingsStoreTests)
- [x] Daily goal achieved notification: SessionNotifier.dailyGoalAchievedBody(goalMinutes:) + sendDailyGoalAchieved(goalMinutes:) fires once per calendar day when today's total focus minutes crosses the configured dailyFocusGoalMinutes; SessionManager.endSession() checks the goal after every session (not just successful ones) using a adia.lastDailyGoalAchievedDate UserDefaults gate; todayDateString() helper; 14 new tests in SessionNotifierTests
- [x] Admin bulk-resend-license endpoint: POST /api/admin/bulk-resend-license re-sends the license welcome email to up to 50 keys at once; dryRun mode previews without sending or writing audit log; skips not_found; writes resend_license audit entries with bulk:true; BulkResendLicensePanel in admin UI with textarea + dry run checkbox + per-key result display; 18 tests (783 → 801)
- [x] Morning nudge notification: SettingsStore.morningNudgeEnabled (default: false) + morningNudgeHour (default: 10, range 6–18); SessionNotifier.morningNudgeBody() (friend-like copy, nonisolated static) + scheduleMorningNudge(hour:) (UNCalendarNotificationTrigger, repeats:false) + cancelMorningNudge() + rescheduleMorningNudge() + scheduleMorningNudgeIfNeeded() (once-per-day UserDefaults gate + hour-passed guard); SessionManager.start() cancels nudge; SessionManager.restoreIfNeeded() schedules nudge on launch; MorningNudgeSection in AccountSettingsTab with toggle + hour picker (formatNudgeHour helper); SettingsView Account tab height 500→540; 5 copy tests in SessionNotifierMorningNudgeTests + 9 settings tests in SettingsStoreTests
- [x] Admin usage-heatmap endpoint: GET /api/admin/usage-heatmap returns 24-bucket per-hour activation counts (UTC) over last N days; ?days= (1–90, default 7), optional ?plan= filter; activationHeatmap() in db.ts (strftime) + db-pg.ts (EXTRACT); UsageHeatmapPanel with bar chart scaled by max; 24 tests (898 → 922)
- [x] Admin revenue endpoint: GET /api/admin/revenue returns MRR, ARR, lifetime revenue, past-due MRR, new MRR (30d), and per-plan breakdowns (active/pastDue/new30d/mrr-or-revenue); revenueMetrics() in db.ts + db-pg.ts + store.ts; RevenueMRRPanel with stat tiles + per-plan table; 25 tests (922 → 947)
- [x] Admin activation-timeline endpoint: GET /api/admin/activation-timeline returns daily new-activation counts over last N days (default 30, max 365); TimelineDay type; activationTimeline() in db.ts + db-pg.ts + store.ts; ActivationTimelinePanel bar chart; ?days= + ?plan= filters; 30 tests (947 → 977)
- [x] Admin cohort-retention endpoint: GET /api/admin/cohort-retention groups licenses by issuance date; returns per-cohort rows (date, total, activeNow, ageDays, retentionRate) + aggregate summary30d/60d/90d tiles; cohortRetention() in db.ts + db-pg.ts + store.ts; CohortRetentionPanel with 3-tile summary + sortable table color-coded by retention rate; ?days= (default 90, max 365) + ?plan= filters; 36 tests (977 → 1013)
- [x] Admin bulk-change-plan tests: 23 tests covering auth (401 no-token, 401 wrong-token, 200 ?token=), validation (missing/empty/>100 keys, missing/invalid plan), core behavior (single key, multiple keys, key normalization, previousPlan capture for all three plan transitions), skip behavior (not_found, already_on_plan, mixed, no mutation), and audit log (one change_plan entry per changed key, detail shape, no entry for not_found/already_on_plan); 1013 → 1036 tests
- [x] Admin bulk-reactivate tests: 20 tests covering auth (401 no-token, 401 wrong-token, 200 ?token=), validation (missing/empty/>100 keys), core behavior (single canceled key, multiple keys, key normalization, previousStatus for canceled/expired/past_due), skip behavior (not_found, already_active, mixed, no mutation), and audit log (one reactivate entry per changed key, detail shape with bulk=true, no entry for not_found/already_active); 1036 → 1056 tests
- [x] Churn analysis endpoint: GET /api/admin/churn-analysis returns daily churn event counts (revoke + set_status to non-active) over last N days; totalChurned, byPlan breakdown, churnRate; ChurnAnalysisPanel with summary tiles + red bar chart; ?days= (default 30, max 365) + ?plan= filters; churnAnalysis() in db.ts + db-pg.ts + store.ts; 33 tests (1056 → 1089)
- [x] Churn cohort breakdown endpoint: GET /api/admin/churn-cohort groups licenses by issuance date; per-cohort rows (date, total, churned, churnedWithin30d/60d/90d, churnRate, ageDays) + aggregate summary30d/60d/90d (cohortCount, totalLicenses, totalChurned, avgChurnRate); churnCohort() in db.ts + churnCohortPg() in db-pg.ts + store.ts; ChurnCohortPanel with 3-tile summary row + scrollable color-coded table; churnRateColor() helper (green <10%, yellow <25%, red >=25%); ?days= (default 90, max 365) + ?plan= filters; 42 tests (1089 → 1131)
- [x] Morning nudge copy variants: SessionNotifier.morningNudgeMessages — 7-entry static pool of friend-like nudge messages; morningNudgeBody() picks randomly via randomElement(); tests refactored from single-call checks to full-pool property checks (poolIsNonEmpty, hasAtLeastThreeVariants, allVariantsAreUnique, noneAreEmpty, noneUseCorporateLanguage, allReferenceStartingWork, noneUsePunishingLanguage, noneContainAllCapsWords — 9 tests replacing 5); web tests unchanged at 1131
- [x] Daily goal gate unit tests: extract fireDailyGoalNotificationIfNeeded(todayMinutes:goal:defaults:) from endSession() as an internal @discardableResult method; 12 new Swift tests in SessionManagerTests — todayDateString format/content/consistency (4 tests), gate returns false below goal, true at/above goal, writes UserDefaults key on first fire, returns false on second call same day, does not write defaults when below goal, re-fires when stored date is stale, edge case goal=0 (8 tests); web tests unchanged at 1131
- [x] Time-to-churn distribution: GET /api/admin/time-to-churn returns histogram of days-from-issuance to first churn event; buckets early (0-7d), mid (8-30d), late (31-90d), very late (91d+) with count + pct; medianDays + meanDays summary stats; TimeToChurnPanel bar chart in admin UI; ?days= + ?plan= filters; 41 tests (1131 → 1172)
- [x] Streak milestone gate unit tests: extract fireStreakMilestoneNotificationIfNeeded(streak:defaults:) from endSession() as an internal @discardableResult method; 10 new Swift tests in SessionManagerTests — non-milestone returns false, all 5 milestones fire, already-notified guard prevents double-fire, higher milestone passes through lower guard, defaults key written on fire, second call same milestone blocked, no defaults write for non-milestone, re-earnable after break reset (key=0), streak=0 not a milestone
- [x] Morning nudge title variants pool: SessionNotifier.morningNudgeTitles — 5-entry static pool of short direct titles; morningNudgeTitle() picks randomly via randomElement(); scheduleMorningNudge uses morningNudgeTitle() replacing fixed "haven't started yet"; 8 pool-property tests in new SessionNotifierMorningNudgeTitleTests suite (poolIsNonEmpty, hasAtLeastThreeVariants, allVariantsAreUnique, returnsNonEmptyString, noneAreEmpty, noneUseCorporateLanguage, noneUsePunishingLanguage, noneContainAllCapsWords)
- [x] Morning nudge AppLogger: scheduleMorningNudgeIfNeeded logs AppLogger.info("notifier.morning_nudge_scheduled", ["hour": hour]) after scheduling, matching the error-logging pattern already present for scheduling failures
- [x] checkStreakBreak unit tests: extract fireStreakBrokenNotificationIfNeeded(lastActiveStreak:currentStreak:defaults:) from checkStreakBreak() as an internal @discardableResult method; 13 new Swift tests in SessionManagerTests — gate returns false when lastActive=0, false when currentStreak>0, true for ≥7-day broken streak, false for <7-day broken streak, both UserDefaults keys reset on any break, keys not written when streak not broken, re-fires on next break (no permanent gate), streak=1–6 (below threshold) resets keys but no notification, large streak fires and resets keys, lastActive=6 exactly at boundary (no fire), milestones re-earnable after break reset
- [x] Analytics endpoint rate-limit tests: 3 tests each (429-at-cap, Retry-After header, rate-limit-before-auth) for churn-analysis, usage-heatmap, revenue, activation-timeline, cohort-retention, churn-cohort — all six endpoints confirmed gated by adminGuard 20 req/60s; 1178 → 1196 tests
- [x] Morning nudge title pool expansion + AppLogger title field: morningNudgeTitles grows 5→7 with "today's untouched" + "no work yet today"; scheduleMorningNudge captures chosen title into local var, logs ["hour": hour, "title": title] consolidating the log previously split across scheduleMorningNudge/scheduleMorningNudgeIfNeeded; 2 new tests (hasSevenVariants, allVariantsAreLowercase) in SessionNotifierMorningNudgeTitleTests; 123 → 125 Swift tests
- [x] Dormant-churn endpoint: GET /api/admin/dormant-churn returns licenses that had ≥1 activation AND a churn event (revoke or set_status→canceled/expired/past_due) within the last N days; DormantChurnLicense type in db.ts + db-pg.ts; store.ts dispatcher; dormant-churn/route.ts; DormantChurnPanel in admin UI with plan/status/days filters + CSV export + status badge coloring; 39 tests (1196 → 1235)
- [x] Dormant-churn CSV rate-limit test: 4th rate-limit test in admin-dormant-churn.test.ts — confirms ?format=csv request after bucket exhausted returns 429 not 400 (rate-limit fires before format parsing); 1235 → 1236 tests
- [x] HistoryInsightsSection.streakBreakChipLabel extraction: extract inline streak-break chip logic into static internal func streakBreakChipLabel(totalBreaks:mostBroken:) on HistoryInsightsSection; 7 Swift tests in HistoryInsightsSectionTests covering hidden (0 breaks), break-count label/value, fragile-streak label/value, edge cases
- [x] Fitness keyword expansion in extractTaskKeyword: add exercise/exercises/exercising, running, training session/plan phrases to the fitness keyword branch (workout/gym/cardio already present)
- [x] Fitness keyword unit tests: dedicated @Test functions covering exercise/exercising, running, "training session", "training plan" + false-positive guard ("running a python script" → code, "marathon training" → fitness); ~10 tests in CalloutManagerTests
- [x] Admin license-timeline endpoint: GET /api/admin/license-timeline?key= returns full paginated audit log for one license (offset/limit), ?format=csv exports all; built on listAuditLog+countAuditLog+listAllAuditLog+findLicense; 21 tests (1236 → 1257)
- [x] Randomized idle notch suggestion rotation: SuggestedSessionTemplates.randomSuggestions(count:excluding:) shuffles catalog; IdleNotchView draws fresh random 3 on each launch/session-end via @State displayedSuggestions; dismiss stays stable (removes from local state); re-enable re-shuffles; 5 Swift tests for randomSuggestions
- [x] SettingsStore.streakBreakCountsDict @Published + HistoryTab reactive wiring: make streakBreakCountsDict @Published so objectWillChange fires on incrementStreakBreak/resetStreakBreaks; HistoryTab gains @ObservedObject private var settings = SettingsStore.shared + reads settings.totalStreakBreaks/mostBrokenStreakLength instead of SettingsStore.shared — streak-break chip now updates live if a session ends while the History tab is open
- [x] LookupPanel → LicenseTimelinePanel cross-link: "View full timeline →" button in LookupPanel result card auto-populates LicenseTimelinePanel; autoTimelineKey state added to Admin; LicenseTimelinePanel gains autoKey/onAutoKeyConsumed props + useEffect (mirrors LookupPanel autoKey pattern) + panelRef scroll-into-view
- [x] Journaling keyword + callout pool + templates: extractTaskKeyword gains "journaling" branch (journal/journaling/diary/morning pages/daily log); CalloutMessages adds 3-tier journalingCallouts pool (4/3/3 messages); SuggestedSessionTemplates gains "Write today's journal entry" + "Listen to and annotate an audiobook chapter"; 92 new tests in CalloutManagerTests + 4 new tests in SuggestedSessionTemplatesTests (344 total)
- [x] Legal keyword + callout pool + templates: extractTaskKeyword gains "legal" branch (brief/briefs, pleading/pleadings, contract/contracts, moot court, bar exam, law review, legal research, legal writing, legal memo, litigation/motions); CalloutMessages adds 3-tier legalCallouts pool (4/3/3 messages); SuggestedSessionTemplates gains "Write a case brief for class" + "Prep for the bar exam — one subject block"; scheduleMorningNudgeIfNeeded logs AppLogger.info when skipping (already_scheduled or hour_passed) with todayStr for production debuggability; 14 new Swift tests in CalloutManagerTests + 4 new tests in SuggestedSessionTemplatesTests (361 total)
- [x] Pre-med keyword + callout pool + templates: extractTaskKeyword gains "premed" branch (anatomy/physiology/biochemistry/pharmacology/pathology/histology/microbiology/immunology/embryology, MCAT/NCLEX/USMLE, med school/pre-med, dissection/cadaver, clinical rotation/skills, anatomy lab/notes, Step 1/2/3); CalloutMessages adds 3-tier premedCallouts pool (4/3/3 messages); SuggestedSessionTemplates gains "Study for the MCAT" + "Review anatomy for lab practical" (catalog 24→26); creative brief false-positive guard (creative brief/marketing brief → writing, design brief → design); journaling tier-4 + tier-0 edge-case tests; 22 new Swift tests in CalloutManagerTests + 4 new tests in SuggestedSessionTemplatesTests (387 total)
- [x] Architecture keyword + callout pool + templates: extractTaskKeyword gains "architecture" branch (architect/architecture/architectural, AutoCAD/Revit/Rhino/Grasshopper/SketchUp, blueprint/floor plan/site plan/elevation/rendering/3D model, construction document, design studio/studio crit/pin-up, ARE exam); software-architecture/system-architecture/cloud-architecture false-positive guard; CalloutMessages adds 3-tier architectureCallouts pool (4/3/3 messages); SuggestedSessionTemplates gains "Work on my architecture studio project" + "Study for my architecture licensing exam" (catalog 26→28); 18 new Swift tests in CalloutManagerTests + 4 new tests in SuggestedSessionTemplatesTests (409 total)
- [x] Startup/entrepreneurship keyword + callout pool + templates: extractTaskKeyword gains "startup" branch (positioned before presentation so "pitch deck" fires before word("deck")); matches pitch deck/investor deck/go-to-market/GTM strategy/business plan/business model/lean canvas/value proposition/seed-series-a-b round/angel investor/unit economics/customer discovery/product-market fit/growth strategy/growth hacking/minimum viable product/fundraising/startup/cofounder/co-founder/SaaS/B2B/B2C; false-positive guards (essay/paper fire first); CalloutMessages adds 3-tier startupCallouts pool (4/3/3 messages, investor/founder voice); SuggestedSessionTemplates grows 28→30 ("Write my startup's pitch deck" 60 min + "Draft a section of my business plan" 45 min); 18 new tests in CalloutManagerTests (401→419) + 4 new tests in SuggestedSessionTemplatesTests (46→50 + ≥30 count guard)
- [x] Nursing keyword + callout pool + templates: extractTaskKeyword gains "nursing" branch (positioned after premed so NCLEX/anatomy stay in premed); matches nursing word, care plan/care plans, nursing notes/assessment/theory/diagnosis, clinical documentation, nurse charting, shift notes/report, dosage calculation/med calc/medication calculation, nursing school/program/class/course, vital signs/vitals, patient assessment/patient care plan, wound care, iv insertion; CalloutMessages adds 3-tier nursingCallouts pool (4/3/3 messages, bedside/care-plan voice); SuggestedSessionTemplates grows 30→32 ("Write a nursing care plan for class" 45 min + "Practice dosage calculations and medication math" 30 min); 14 new tests in CalloutManagerTests (419→433) + 4 new tests in SuggestedSessionTemplatesTests (50→54)
- [x] Photography keyword + callout pool + templates: extractTaskKeyword gains "photography" branch positioned BEFORE video so photo editing/raw editing tasks don't mis-route via word("editing"); matches lightroom, photography/photographer, photoshoot/photo shoot, photo editing/edit photos/editing photos, capture one, portrait/landscape/street/product photography, darkroom, headshots, photo series/project, raw files/raw editing; CalloutMessages adds 3-tier photographyCallouts pool (4/3/3 messages, client-delivery voice: "those photos aren't going to edit themselves" / "CLOSE THIS. open your RAW files."); SuggestedSessionTemplates grows 32→34 ("Edit and export photos from my shoot" 45 min + "Practice and study photography composition techniques" 30 min); 15 new Swift tests in CalloutManagerTests (433→448) + 4 new tests in SuggestedSessionTemplatesTests (54→58)
- [x] Data science / ML keyword + callout pool + templates: extractTaskKeyword gains "datascience" branch positioned BEFORE research (removes data science/data scientist from research branch); matches machine learning/deep learning/neural network/pytorch/tensorflow/keras/scikit-learn/sklearn/xgboost/jupyter/kaggle/data science/data scientist/natural language processing/computer vision/reinforcement learning/model training/hyperparameter/training loss/gradient descent/gradient boosting/random forest/decision tree; CalloutMessages adds 3-tier datascienceCallouts pool (4/3/3 messages, notebook/model voice: "your model isn't going to train itself." / "CLOSE THIS. open your jupyter notebook."); SuggestedSessionTemplates grows 34→36 ("Train and evaluate a machine learning model" 60 min + "Work through a Kaggle notebook or data science project" 45 min); 16 new Swift tests in CalloutManagerTests (448→464) + 4 new tests in SuggestedSessionTemplatesTests (58→62)
- [x] Game dev keyword + callout pool + templates: extractTaskKeyword gains "gamedev" branch (positioned BEFORE code so unity/godot don't fall through); matches unity/godot/unreal engine/game dev/game development/game design document/gdd/game jam/game engine/level design/level editor/game mechanic/game mechanics/pygame/phaser/libgdx/gdevelop; false-positive guard: "game plan" not matched; CalloutMessages adds 3-tier gamedevCallouts pool (4/3/3 messages, engine/jam voice: "your game isn't going to build itself." / "CLOSE THIS. open Unity."); SuggestedSessionTemplates grows 36→38 ("Build my game in Unity or Godot" 60 min + "Write my game design document" 45 min); 11 new Swift tests in CalloutManagerTests (464→475) + 4 new tests in SuggestedSessionTemplatesTests (62→66)
- [x] Engineering keyword + callout pool + templates: extractTaskKeyword gains "engineering" branch (positioned BEFORE datascience/research so solidworks/arduino/pcb don't fall through via word("lab")); matches solidworks/fusion 360/ansys/microcontroller/arduino/raspberry pi/pcb/circuit board/circuit diagram/circuit design/mechanical engineering/electrical engineering/civil engineering/chemical engineering/biomedical engineering/aerospace engineering/computer engineering/finite element/fea/heat transfer/fluid dynamics/fluid mechanics/thermodynamics/structural analysis/strength of materials/machine design/statics lab/engineering lab/engineering report; CalloutMessages adds 3-tier engineeringCallouts pool (4/3/3 messages, CAD/lab voice: "close this and open your CAD file." / "CLOSE THIS. open SolidWorks."); SuggestedSessionTemplates grows 38→40 ("Complete my engineering problem set" 60 min + "Work on my CAD model or technical drawing" 45 min); 9 new Swift tests in CalloutManagerTests (475→484) + 3 new tests in SuggestedSessionTemplatesTests (66→69)
- [x] Therapy/counseling keyword + callout pool + templates: extractTaskKeyword gains "therapy" branch (positioned AFTER nursing, BEFORE legal; prevents "counseling" from falling through to tutor via word("coaching")); matches therapy notes/session notes/progress notes/treatment plan/case conceptualization/case formulation/intake notes/clinical notes/therapist/counseling/counselor/lcsw/lmft/lpc/mft/cbt worksheets/dbt skills/exposure therapy/cognitive behavioral/social work/clinical hours/supervision notes/client notes/case notes/mental health notes; CalloutMessages adds 3-tier therapyCallouts pool (4/3/3 messages, client/supervision voice: "your clients deserve your focus." / "CLOSE THIS. open your case notes."); SuggestedSessionTemplates grows 40→42 ("Write up my therapy session notes" 30 min + "Work on my CBT worksheets or treatment planning" 45 min); 10 new Swift tests in CalloutManagerTests (484→494) + 3 new tests in SuggestedSessionTemplatesTests (69→72)
- [x] Social science + nutrition keyword branches, callout pools, and templates: extractTaskKeyword gains "socialscience" branch (positioned after therapy, before legal; catches political science/poli sci/anthropology/ethnography/criminology/criminal justice/LSAT/pre-law/public policy/public administration/comparative politics/international relations; sociology stays in studying branch); gains "nutrition" branch (positioned after fitness; catches dietitian/dietician/nutritionist/macronutrients/food science/food journal/dietary analysis/dietary intake/nutritional science/clinical nutrition/nutrition assessment/calorie tracking/calorie counting; nutrition plan + meal prep stay in fitness); CalloutMessages adds 3-tier socialScienceCallouts (4/3/3 messages, poli sci/LSAT voice) + 3-tier nutritionCallouts (4/3/3 messages, dietetics/food journal voice); SuggestedSessionTemplates grows 42->46 ("Work on my political science or sociology paper" 60 min + "Prepare for the LSAT" 90 min + "Complete my dietetics or food science assignment" 45 min + "Track and analyze my daily nutrition intake" 30 min); 21 new Swift tests in CalloutManagerTests (494->515) + 7 new tests in SuggestedSessionTemplatesTests (72->79)
- [x] Culinary + philosophy keyword branches, callout pools, and templates: extractTaskKeyword gains "culinary" branch (positioned after nutrition; catches culinary/culinary school/culinary arts/program/class/technique, recipe development/testing/creation/writing, baking, pastry/pastry arts/school/class, mise en place, knife skills, plating technique, flavor profile, sauce development, menu development/planning, cooking class/technique, gastronomy); gains "philosophy" branch (positioned after socialscience, before legal; catches philosophy/philosophical/philosopher, Kant/Plato/Socrates/Aristotle/Nietzsche/Descartes/Hume/Locke/Hegel, metaphysics/epistemology/ontology, moral philosophy/political philosophy/philosophy of mind/of science, ethics paper/essay, thought experiment, argument analysis/philosophical argument, logic problem, dialectic, philosophical inquiry, philosophy class/course/paper, utilitarianism/deontology/consequentialism); CalloutMessages adds 3-tier culinaryCallouts (4/3/3: "those recipes aren't going to test themselves." / "CLOSE THIS. open your recipe notes.") + 3-tier philosophyCallouts (4/3/3: "Kant didn't write the Critique by scrolling." / "CLOSE THIS. open your philosophy notes."); SuggestedSessionTemplates grows 46→50 ("Develop and test a new recipe" 45 min + "Study for a culinary school exam or technique quiz" 30 min + "Analyze a philosophical argument or write a response paper" 60 min + "Read and take notes on a philosophy text" 45 min); 24 new Swift tests in CalloutManagerTests (515→539) + 7 new tests in SuggestedSessionTemplatesTests (79→86)
- [x] Music production/theory keyword split: extractTaskKeyword splits "music" into "musicproduction" (DAW apps/beatmaking/mixing/mastering/songwriting/composing/recording) and "musictheory" (music theory/ear training/sight reading/chord progressions/counterpoint/aural skills); old "music" dispatch replaced with two cases; musicProductionCallouts (4/3/3: "your track isn't going to mix itself." / "CLOSE THIS. Open your DAW.") + musicTheoryCallouts (4/3/3: "your ear won't train itself — close this." / "CLOSE THIS. Do your ear training."); SuggestedSessionTemplates grows 50→52 ("Produce and mix a track in my DAW" 60 min + "Practice ear training and music theory exercises" 30 min); old "music" tests updated to "musicproduction"; 16 new tests (539→552 CalloutManagerTests) + 7 new tests (86→93 SuggestedSessionTemplatesTests)
- [x] Environmental science keyword + callout pool + templates: extractTaskKeyword gains "enviro" branch (positioned after music blocks; catches environmental science/studies/policy/impact/chemistry/biology, ecology/ecologist/ecosystem, conservation biology, climate change/science/policy/model, sustainability, carbon footprint, greenhouse gas, biodiversity, deforestation, habitat loss, env sci); enviroCallouts (4/3/3: "the ecosystem isn't going to study itself." / "CLOSE THIS. Open your field notes."); SuggestedSessionTemplates grows 52→54 ("Write a field ecology or environmental science report" 60 min + "Study for my environmental science exam" 45 min); 6 new tests in CalloutManagerTests; 6 new tests in SuggestedSessionTemplatesTests (≥54 count guard added)
- [x] UX design keyword + callout pool + templates + information-architecture false-positive fix: extractTaskKeyword gains "ux" branch positioned BEFORE the design branch; catches user research/usability testing/user flow/user journey/journey map/affinity map/affinity diagram/user persona/design thinking/HCI/human-computer interaction/user testing/UX research+design+writing/information architecture/interaction design/accessibility audit/wireframing/user story; "information architecture" also added to isSoftwareArchitecture guard so building-architecture callouts never fire for UX tasks; uxCallouts(tier:) 4/3/3 messages (user-research/Figma voice: "your user research isn't going to conduct itself." / "CLOSE THIS. open your design tool."); SuggestedSessionTemplates grows 58→60 ("Conduct user research and synthesize findings" 45 min + "Map out a user flow or wireframe a feature in Figma" 45 min); 20 new tests in CalloutManagerTests (577→597) + 4 new tests in SuggestedSessionTemplatesTests (100→104)
- [x] Statistics keyword + callout pool + templates: extractTaskKeyword gains "statistics" branch positioned BEFORE studying (bare word("statistics")/word("stats") stay in studying for generic academic use); catches rstudio/r studio, spss, stata, hypothesis testing/test, linear/logistic/multiple regression, regression analysis, multivariate analysis, anova, t-test, chi-square, confidence interval, statistical analysis/modeling/model, descriptive/inferential statistics, p-value/p value, effect size, sample size calculation; statisticsCallouts(tier:) 4/3/3 (software/analysis voice: "those regressions won't run themselves." / "CLOSE THIS. open your stats software."); SuggestedSessionTemplates grows 60→62 ("Run a statistical analysis in R or SPSS" 60 min + "Complete a statistics problem set or lab report" 45 min); 17 new tests in CalloutManagerTests (597→614) + 3 new tests in SuggestedSessionTemplatesTests (104→107)
- [x] Kinesiology/sports science keyword + callout pool + templates: extractTaskKeyword gains "kinesiology" branch positioned BEFORE fitness (catches exercise physiology, biomechanics, sports science, CSCS exam, motor control, gait/movement analysis, physical therapy, physiotherapy, athletic training, strength and conditioning, sport/sports psychology, human movement, musculoskeletal); kinesiologyCallouts(tier:) 4/3/3 (PT/sports-science voice: "those movement analyses won't write themselves." / "CLOSE THIS. open your kinesiology notes."); SuggestedSessionTemplates grows 62→64 ("Complete my kinesiology or exercise physiology assignment" 60 min + "Study for the CSCS exam or kinesiology test" 60 min); 14 new tests in CalloutManagerTests (614→628) + 3 new tests in SuggestedSessionTemplatesTests (107→110)
- [x] Veterinary/animal science keyword + callout pool + templates: extractTaskKeyword gains "veterinary" branch positioned BEFORE premed (catches veterinary/veterinarian, vet school/medicine/tech/technician, veterinary medicine/pathology/surgery/clinic, animal science/behavior/physiology, navle, zoology, comparative anatomy, large/small animal, animal hospital); veterinaryCallouts(tier:) 4/3/3 (clinical voice: "those case notes aren't going to write themselves." / "CLOSE THIS. open your veterinary notes." / "no one passes NAVLE by scrolling."); SuggestedSessionTemplates grows 64→66 ("Complete my veterinary school assignment or case notes" 45 min + "Study for the NAVLE or veterinary school exam" 90 min); 14 new tests in CalloutManagerTests (628→642) + 4 new tests in SuggestedSessionTemplatesTests (110→114 + ≥66 count guard)
- [x] Business/management keyword + callout pool + templates: extractTaskKeyword gains "business" branch positioned BEFORE research so "marketing research"/"market analysis" route here (MBA, GMAT, case analysis, business case, operations management, supply chain, organizational behavior, strategic management, business strategy, marketing research, market research, market analysis, market segmentation, human resources, hr management, management consulting, consulting case, competitive analysis, competitor analysis, consumer behavior, brand management, brand strategy, corporate strategy, business administration, SWOT analysis, value chain, business school, management class/course); startup branch above still catches "business plan"/"pitch deck" first; businessCallouts(tier:) 4/3/3 ("your case analysis isn't going to write itself." / "McKinsey recruits don't get there by browsing." / "CLOSE THIS. open your case analysis." / "no one gets their MBA by scrolling."); SuggestedSessionTemplates grows 66→68 ("Work on an MBA case analysis or strategic management assignment" 60 min + "Prep for the GMAT or business school entrance exam" 90 min); 17 new tests in CalloutManagerTests (642→659) + 4 new tests in SuggestedSessionTemplatesTests (114→118 + ≥68 count guard)
- [x] Finance keyword expansion (investment banking / securities terms): finance branch gains DCF (discounted cash flow/dcf model/word("dcf")), LBO (leveraged buyout/word("lbo")), Series 7/63, FINRA, Bloomberg Terminal, comparable company/companies/comps analysis, investment banking/ib analyst, equity research, valuation model/company valuation, ACCA, CIMA, financial due diligence, mergers and acquisitions/m&a analysis; financeCallouts updated with IB voice ("your DCF model isn't going to build itself." / "no one cracks investment banking by scrolling." / "your LBO assumptions won't fill themselves."); SuggestedSessionTemplates grows 68→69 ("Build a DCF model or investment banking analysis" 60 min); 10 new tests in CalloutManagerTests (659→669) + 3 new tests in SuggestedSessionTemplatesTests (118→121 + ≥69 count guard)
- [x] Paramedicine/EMT keyword + callout pool + templates: extractTaskKeyword gains "paramedicine" branch positioned BEFORE nursing (so NREMT/EMS terms don't fall into nursing); matches word("emt")/word("paramedic")/word("paramedicine"), nremt, aemt, emergency medical technician, ems protocol/training/class/certification/exam/program, pre-hospital/prehospital, trauma assessment, field triage, basic/advanced life support, acls, pals, cpr certification, airway management; paramedicineCallouts(tier:) 4/3/3 ("those protocols aren't going to memorize themselves." / "CLOSE THIS. open your paramedic notes." / "no one passes the NREMT by scrolling."); SuggestedSessionTemplates grows 71→73 ("Study for the NREMT or EMT certification exam" 60 min + "Complete my paramedic or EMS training assignment" 45 min); 15 new tests in CalloutManagerTests (669→684) + 3 new tests in SuggestedSessionTemplatesTests (121→124 + ≥73 count guard)
- [x] Social work keyword split from therapy: "social work" removed from therapy branch and owned by new "socialwork" branch positioned BEFORE therapy; matches lower.contains("social work"), "social worker"/"social workers", word("msw")/word("bsw")/word("lmsw"), case management, casework, child protective services, child welfare, community resources, family services, social services, intake assessment, social welfare, welfare policy, "field placement" + "social"; socialworkCallouts(tier:) 4/3/3 ("your clients need you focused — get back to your case notes." / "CLOSE THIS. open your case notes." / "no one earns their MSW by scrolling."); SuggestedSessionTemplates grows 73→75 ("Write case notes or intake assessment for field placement" 45 min + "Study for my social work licensing exam or MSW coursework" 60 min); 13 new tests in CalloutManagerTests (684→697) + 3 new tests in SuggestedSessionTemplatesTests (124→127 + ≥75 count guard)
- [x] Occupational therapy keyword + callout pool + templates: extractTaskKeyword gains "occupationaltherapy" branch positioned AFTER therapy and BEFORE publicheath; matches lower.contains("occupational therapy"/"occupational therapist"/"occupational therapists"), word("nbcot"), activities of daily living, word("adls")/word("adl"), hand therapy, pediatric ot, sensory integration, sensory processing, fine motor skills, adaptive equipment, ot fieldwork/placement/school/program/class/exam; occupationaltherapyCallouts(tier:) 4/3/3 ("your OT notes aren't going to write themselves." / "CLOSE THIS. open your OT notes." / "no one passes the NBCOT by scrolling."); SuggestedSessionTemplates grows 75→77 ("Write up my OT session notes or treatment plan" 30 min + "Study for the NBCOT or occupational therapy school exam" 60 min); 13 new tests in CalloutManagerTests (697→710) + 3 new tests in SuggestedSessionTemplatesTests (127→130 + ≥77 count guard)
