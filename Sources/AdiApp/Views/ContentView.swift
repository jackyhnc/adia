import SwiftUI

// Placeholder required for the Settings scene in AdiApp.swift.
// The real UI lives in NotchView inside the NSPanel.
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
