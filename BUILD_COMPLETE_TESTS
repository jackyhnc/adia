Test suite build complete.

## Swift Tests (Tests/AdiTests/)
- LicenseManagerTests — state machine (unknown/trial/trialExpired/licensed), offline grace,
  deactivate clears Keychain, isUsable truth table. Internal helpers: resetForTesting,
  _setTrialStartDateForTesting, _injectLicenseForTesting.
- SettingsStoreTests — setAPIKey Keychain round-trip, empty/whitespace clearing, hasAPIKey.
- ConversationManagerTests — start(mode:) opening messages, reset clears all state,
  parseAccessDecision static pure helper (GRANTED/DENIED/nil).
- LocalBlockServerTests — start/stop lifecycle, stop-twice safety, double-start
  releases prior listener. Internal: init(forTesting:).
- OnTaskDetectorTests — onTask when no session, onTask when isConfigured=false,
  rate-limit caching within minInterval, cache expiry. Internal: _isConfiguredOverride,
  _setLastEvaluatedAtForTesting, _setLastStatusForTesting.

## Web Tests (web/__tests__/) — 37 tests, all green
- license.test.ts — key format /^ADIA-[A-Z2-9]{4}-.../, 1000-call uniqueness, planExpiry
- db.test.ts — per-test SQLite isolation via resetDbForTesting(), round-trip, case lookup,
  seat counting, stripe_session idempotency, setStatus, joinWaitlist dedup
- activate.test.ts — 400/404/403-expired/403-seats/200
- validate.test.ts — 400/404/403-expired/200
- waitlist.test.ts — invalid email → 307/invalid, valid → 307/ok, dedup
- checkout.test.ts — redirects to /pricing?stripe=unconfigured when no key
- webhook.test.ts — 503 unconfigured, 400 missing signature (happy-path TODO)

## CI (.github/workflows/ci.yml)
- swift-test: macos-15, swift test
- web-test: ubuntu, npm ci && npm run test:run
- pipeline-smoke: macos-15, bash scripts/test-pipeline.sh

## Scripts
- scripts/test-pipeline.sh — build-app, sign, notarize (skips without creds),
  build-dmg, DMG + sha256 existence check, codesign verify
